# Fetch the website (will fail if already exists)
git clone git@github.com:sexxis/website

# Set up reverse proxy port
router register sexxi.xyz 4200

# Run the update server (which will fork a sexxi.xyz process)
router create sexxi.clive.io
